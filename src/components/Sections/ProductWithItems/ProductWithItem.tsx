import './product-with-items.scss';

import PreItemsList from '../../PreItemsList/PreItemsList';

const ProductWithItem = () => {
	return (
		<section className='section'>
			<div className='container'>
				<PreItemsList />
			</div>
		</section>
	);
};

export default ProductWithItem;
